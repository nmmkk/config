SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE:-${(%):-%N}}")"; pwd)"

conf_dir=($SCRIPT_DIR/../stow-get/conf ${conf_dir[@]})
packages=(git tmux bash fish-shell zsh python3 vim emacs global)

set_inst_dir $XDG_DATA_HOME/stow-get/usr/local

